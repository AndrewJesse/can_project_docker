{
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.0.12"
  },
  "engines": {
    "node": "20.x"
  }
}